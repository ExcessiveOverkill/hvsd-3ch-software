/*
 * Generated by AURA device-firmware-gen — do not edit by hand.
 * inline device-side accessors — zero call overhead after optimisation
 */

#pragma once

#include "reg_storage.hpp"
#include <cstring>

namespace regs {

    // --- Register accessors --------------------------------------
    // status — unsigned 8-bit | read-only
    inline uint8_t get_status() { return static_cast<uint8_t>(regs.status.value); }
    inline void set_status(uint8_t v) { regs.status.value = static_cast<uint32_t>(v); }

    // control — unsigned 8-bit | read-write
    inline uint8_t get_control() { return static_cast<uint8_t>(regs.control.value); }
    inline void set_control(uint8_t v) { regs.control.value = static_cast<uint32_t>(v); }

    // fan/rpm_fbk — unsigned 16-bit | read-only
    //   Fan speed in RPM
    inline uint16_t get_fan_rpm_fbk() { return static_cast<uint16_t>(regs.fan.rpm_fbk.value); }
    inline void set_fan_rpm_fbk(uint16_t v) { regs.fan.rpm_fbk.value = static_cast<uint32_t>(v); }

    // fan/pwm_cmd — unsigned 8-bit | read-write
    //   Fan PWM command (0-100%)
    //   min=0, max=100, default=0
    inline uint8_t get_fan_pwm_cmd() { return static_cast<uint8_t>(regs.fan.pwm_cmd.value); }
    inline void set_fan_pwm_cmd(uint8_t v) { regs.fan.pwm_cmd.value = static_cast<uint32_t>(v); }

    // motor: count=3 — access via regs.motor[i]...

} // namespace regs
